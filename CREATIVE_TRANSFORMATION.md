# Arbor 创造性改造方案（第三份：范式级改造）

> 配套前两份：`OPTIMIZATION_FINDINGS.md`（28 个增量优化——把现有机制跑快）、`OPTIMIZATION_INNOVATIVE.md`（27 个结构性创新——换一种机制）。
> 这份是**第三层**：改变 **Arbor 是什么**——从"给单个指标做优化的执行器"升级为"持续累积理解、自我修正信念、产出可引用知识的研究共同体"。
> 文件路径相对 `src/` 根目录；行号为当前快照（2026-08-12）。

---

## 0. 一句话总览

> **把 Arbor 从"分数优化器"升级为"科学共同体"：让每棵树长成一份可引用的论文，让每次运行留下校准过的信念，让失败变成先验，让人类成为首席研究员。**

与 E1–E27 的分界（为什么这份叫"创造性改造"而不是第三份优化清单）：

- **E 系列**：系统的**内部效率**——怎么跑得更快、更省、更聪明地选择（机制层）。
- **本方案**：系统的**认知形态**——它理解什么、如何自我修正、如何与人协作、产出什么形态的知识（范式层）。E 系列回答"循环怎么转"，本方案回答"循环在理解什么、留下什么"。

---

## 1. 七个方向的定位（总览图）

```
┌─────────────────────── 现在的 Arbor（优化器） ───────────────────────┐
│ 单次运行 · 单指标 · 点分数 · 文本记忆 · 单协调器 · 静态报告 · 只读UI  │
└──────────────────────────────────────────────────────────────────────┘
                              │  七个创造性方向
                              ▼
┌─────────────────────── 改造后的 Arbor（科学共同体） ──────────────────┐
│ D1 理论层+校准：树从"假设清单"→"信念网络"，预测-惊奇驱动探索          │
│ D2 科研项目模式：树跨 run 存活，开放问题/里程碑/证据时间衰减          │
│ D3 统计严谨层：点分数 → 分布/效应量/功效分析，合并门变统计检验        │
│ D4 对抗式科研：红队 executor + 失败博物馆，合并要"打不破"             │
│ D5 自进化闭环：元循环优化自身 prompt/策略 + 小模型蒸馏                │
│ D6 研究组模式：多人格协调器委员会，多样性对抗早熟收敛                │
│ D7 论文级交付 + PI 导师制：树即论文，人类作为共同研究者双向注入       │
└──────────────────────────────────────────────────────────────────────┘
```

每个方向的本质变化：

| 方向 | 从 → 到 | 改变的"认知形态" |
|------|--------|-----------------|
| D1 | 假设清单 → 信念网络 | 系统对自己想法的把握 |
| D2 | 会话树 → 项目树 | 时间的组织（跨 run 累积） |
| D3 | 点分数 → 统计分布 | 对证据的信任方式 |
| D4 | 单向实现 → 对抗验证 | 对"改进"的判定标准 |
| D5 | 归档轨迹 → 自我进化 | 框架的成长方式 |
| D6 | 单一人格 → 研究组 | 探索多样性的来源 |
| D7 | 静态报告 → 论文+控制台 | 对外产出与对人的关系 |

---

## 2. D1 理论层与预测校准：从"假设树"到"信念网络"

**现在的问题**：`idea_tree.py:28` 的 `Node` 只存 `hypothesis / score / insight`——树记录"试过什么、得了几分"，不记录"为什么应该有效、我有多大把握"。DECIDE 靠 LLM 读文本判断 promising；`convergence.py:24` 只看分数速度。系统**没有自我模型**：它从不预测，因此从不惊讶，也从不发现"我对这类想法的判断有系统性偏差"。

**改造内容**：

- **D1.1 节点携带显式预测**：`Node` 增加 `prediction: {delta_expected, confidence, mechanism}` 字段（扩展 `idea_tree.py:58` 的 `MUTABLE_FIELDS` 白名单）。IDEATE 时强制填写：期望改变多少分、置信度、以及**机制陈述**（"为什么它有效"，与 `hypothesis` 区分：假设是"做什么"，机制是"为什么"）。
- **D1.2 校准账户（calibration ledger）**：新 `calibration.py`——每次实验后把 (预测, 实际) 配对记账；按 idea 类型/子树聚合，算 Brier score 与校准曲线。校准数据注入 OBSERVE 的 constraints block（`prompts.py` 的 `get_constraints_block` 已有此通道）。
- **D1.3 惊奇驱动探索（surprise-driven）**：`|预测−实际|` 超阈值的节点自动生成"为什么？"调查节点（新状态 `investigate`），进入与主循环并行的 INVESTIGATE 通道——系统主动追查异常，而不是记录后忘记。树从"改进记录"变成"理解构建"。
- **D1.4 机制信念传播**：`propagate_insights`（`tree_ops.py:564`）除传播 insight 文本外，传播**机制信念**：兄弟节点共享同一机制时聚合证据，形成"这个代码库里机制 M 的可信度"——祖先 insight 从"文本总结"升级为"信念更新"。

**收益**：选择从"promising"变成"可校准的更新"；系统学会"我在低估数据类想法"这类元知识；惊奇节点让研究自动转向最有信息量的方向（呼应 roadmap 1.5"自主收集更多实验信息"）。

**风险**：强制预测可能退化为模板化猜测；LLM 校准天然有噪声——先离线用现有 replay/events 数据做"预测可行性"回测再上线。

**协同**：与 E2/E5 配合（语义召回给预测提供先验；EI 是 D1.1 的数值化版本）。

**落点**：`idea_tree.py`、`tree_ops.py`、新 `calibration.py`、`prompts.py`。**难度：中**。

---

## 3. D2 科研项目模式：树跨 run 存活

**现在的问题**：一次 run 一棵树（`orchestrator.py` 会话级持久化），run 结束树就归档。experience/findings（`experience.py`）只带走"具体教训"，带不走**开放问题、未验证的机制、半成品分支**。第 10 次跑同一 benchmark 仍然从根重新开始。

**改造内容**：

- **D2.1 树分叉（tree fork）**：`IdeaTree.save/load` 支持"继承父树"——新 run 以旧树为初始状态，`pending` 节点继续可派发，`open_question` 类型节点（D1.3 惊奇节点的正式化）成为新 run 的初始 frontier。把 git worktree 的分支思维用到树上。
- **D2.2 里程碑与预算分配**：`TreeSetMetaTool`（`tree_ops.py:404`）扩展 `milestones`：用户给"研究纲领"（多阶段目标），系统为每个里程碑分配子预算，DECIDE 时按里程碑回报率重分配（呼应 E6/E7，但对象是跨 run 的问题级）。
- **D2.3 证据时间衰减**：旧节点证据在后续 run 中权重随 run 数衰减（`recall.py` 召回时乘 `decay^(run_age)`），新证据自动覆盖旧结论——树是活的，不是档案。
- **D2.4 开放问题面板**：WebUI（`webui/server.py`，现在只读 SSE）增加 Open Questions 面板；报告（`report/generator.py`）输出"下一轮建议"节——把每次 run 变成研究议程的一环。

**收益**：研究真正"累积"；同一 benchmark 的第 N 次运行拥有前 N-1 次的全部结构，而不只是教训文本；支持多阶段、多目标的真实科研项目。这是 roadmap "Direction 1.1 长时程管理"的直接实现。

**风险**：树膨胀（需要归档/剪枝策略，E1 的 embedding 去重可做护栏）；跨 run 时环境可能变化使旧结论失效——时间衰减正是为此。

**落点**：`idea_tree.py`（fork/继承）、`cli/commands/run.py`（`--from-tree` 参数）、`recall.py`、`webui/server.py`、`report/generator.py`。**难度：中高**（数据结构小改，但语义改动大）。

---

## 4. D3 统计严谨层：从点分数到效应量

**现在的问题**：`Node.score` 是单点分数（`idea_tree.py:43`），merge 门是 `score > trunk + merge_threshold` 的标量比较。单次实验的方差（随机 seed、机器噪声）从未被建模——`AB_REPORT.md` 已经实测本机复测偏差巨大（44.1x vs 52.7x）。**当前框架可能把噪声当改进合并，也可能把真改进当噪声剪掉。**

**改造内容**：

- **D3.1 多 seed 评估**：`executor_run.py` 的 eval 阶段支持 `n_seeds`（可配置，默认 1 保持兼容）；`Node` 存 `score_mean / score_std / n`。
- **D3.2 效应量合并门**：merge 条件从"点分数高"改为"均值差 + 置信区间下界 > 0"（Welch t 或 bootstrap）；`BudgetStage.promotion_gate`（`config.py:28` 已定义未接线）接上统计语义。
- **D3.3 功效分析**：派发前用历史 executor 噪声水平估计"这个实验能检测到多大效应"；`RunExecutorParallelTool` 的 input_schema（`executor_run.py:946`）增加 `effect_size_expected` 字段；低于可检测下限的想法标记为"证据不足"而非"失败"。
- **D3.4 噪声登记**：把每次 eval 的环境指纹（机器/CPU/时间戳）记入节点，跨机器合并时提示方差风险。

**收益**：合并决策的科学合法性；剪枝不再误杀黑马；AB 报告里"本机复测偏差"的尴尬变成框架原生能力。

**风险**：多 seed 增加算力成本（每实验 ×k）；小 benchmark 可能 seed 间方差巨大——先测量典型噪声再定默认 n。

**落点**：`idea_tree.py`、`executor_run.py`、`config.py`、`convergence.py`（velocity 改用统计量）。**难度：中，风险低、价值高——建议 P0**。

---

## 5. D4 对抗式科研：红队 executor 与失败博物馆

**现在的问题**：executor 的职责是"实现并改进"，合并门只看分数。**没有任何机制尝试打爆当前最佳方案**——dev 过拟合、测试集泄漏、指标漏洞（例如直接改 eval）只在 contamination probe（`contamination.py`）里被动查一次，LLM 探针还处于 stub 状态。

**改造内容**：

- **D4.1 红队 executor 角色**：新工具 `RunAdversarialExecutor`（挂 `executor_run.py` 旁）——给定 trunk/候选分支，目标不是提分，而是**找到候选失败的方式**：边界用例、对 dev 的过拟合证据、指标漏洞、数据泄漏。输出结构化"攻击报告"。
- **D4.2 对抗合并门**：merge 流程在 dev/test 验证之外增加"对抗验证"：候选必须"打不破"（红队找不到可复现反例），或打破点被记录为**已知限制**写进节点（限制声明成为论文的一部分，见 D7）。
- **D4.3 失败博物馆（failure museum）**：`TreePruneTool`（`tree_ops.py:360`）剪枝时不再丢弃，而是转成**结构化失败档案** `{hypothesis, mechanism, symptom, evidence, why_it_failed}` 进入可检索索引（配合 E2 语义召回）。IDEATE 先查博物馆："这个代码库里，声称 X 的想法通常因为 Y 失败"——失败模式变成先验。
- **D4.4 漏洞规则学习**：红队发现的指标漏洞类型（"改了 eval.py"、"硬编码测试答案"）汇入 `contamination.py` 的规则集，变成自动护栏（在 `core/tools/path_guard.py` 层拦截）。

**收益**：合并的鲁棒性；"刷分"空间被压缩；失败模式从"被遗忘的死路"变成"学习资产"——这是从优化器到科学家的关键一步（科学进步 = 失败模式的累积）。

**风险**：红队 LLM 攻击能力有限（与主模型同源则盲区相似）；额外算力开销——可限制为仅对候选 top-2 分支做对抗验证。

**落点**：`executor_run.py`、`tree_ops.py`、`contamination.py`、`recall.py`。**难度：中**。

---

## 6. D5 自进化闭环：Arbor 优化 Arbor

**现在的问题**：`trajectory.py` 已经导出 SFT/RL 格式轨迹（`build_trajectory:48`、`append_token_record:119`），但**没有任何消费端**——数据写了就归档。`AB_REPORT.md` 证明团队手工做框架 A/B 有效（dev 加速 +221%），但那是人工流程，不是框架能力。

**改造内容**：

- **D5.1 元循环（meta-cycle）**：新 `arbor meta-optimize` 子命令——把 Arbor 自身（coordinator prompt 节、SELECT 策略参数、收敛阈值）作为优化目标，用 arbor-zoo 里的快任务跑 2-3 轮树搜索，自动产出"prompt 补丁"（diff 形式）并 A/B 验证。**这是本仓库正在做的工作的产品化。**
- **D5.2 小模型蒸馏决策**：从 `trajectory.jsonl` 蒸馏本地小模型处理**例行决策**（报告解析、eval 分数抽取、insight 传播的格式转换），大模型只管真正的科研推理——E14 的模型分层升级为"学出来的分层"（用真实轨迹微调，而非拍脑袋选便宜模型）。
- **D5.3 策略参数自调优**：`convergence.py:24` 的阈值（`improvement_threshold`/`parent_exhaustion_count`…）与 `merge_threshold` 进入树搜索空间，用 E6 的分支预算机制自动调。
- **D5.4 人格变体 A/B**：coordinator prompt 的"研究人格"作为变量跑平行 run（`RunExecutorParallelTool` 的思想用在协调器层），赢者人格沉淀为默认。

**收益**：框架自我改进成为持续能力而非人工项目；trajectory 数据从"归档"变"资产"；小模型蒸馏直接省钱（呼应 E14）。

**风险**：元循环可能过拟合自身基准（需要留出基准）；蒸馏质量需验证（"便宜先试，贵重试"双保险，E14 已提）。

**落点**：新 CLI 子命令、`trajectory.py` 消费端、`config.py`、`convergence.py`。**难度：高**，但价值是"框架的框架"。

---

## 7. D6 研究组模式：多人格协调器委员会

**现在的问题**：单一协调器一种人格到底（`orchestrator.py:122` 单 Agent ReAct 循环）；IDEATE 的多样性只靠温度。**早熟收敛**（所有分支都是同一想法的变体）无结构性防御。

**改造内容**：

- **D6.1 人格参数化**：`build_coordinator_system_prompt`（`prompts.py:15`）把"研究风格"做成配置节（`ideation_style: conservative | radical | data_first | literature_driven`）；同一棵树由不同人格的协调器并行提案（共享 `IdeaTree`，`async_update_node` 锁安全已有）。
- **D6.2 委员会合并（dean 环节）**：每 N 个节点一次轻量 LLM 调用，把不同人格的待办叶子按**互补性**重排：抑制同类想法扎堆，鼓励正交方向（配合 E1 的 embedding 距离做量化去重）。
- **D6.3 多样性度量**：新 `diversity.py` 计算树级"假设多样性指数"（叶子假设两两 embedding 距离的均值），注入 OBSERVE；跌破阈值时主动切换人格或强制生成"对立假设"（E5 的 EI 补一个多样性项）。

**收益**：结构性防御早熟收敛；同一预算覆盖更宽的假设空间；天然支持 D5.4 的人格 A/B。

**风险**：多协调器成本上升（E27 已讨论并发 merge 冲突）；同一模型下人格区分度可能有限——建议人格差异 = system prompt + 温度 + 工具偏好三重编码。

**落点**：`prompts.py`、`orchestrator.py`、新 `diversity.py`、`tree_ops.py`。**难度：高**，建议排在 E27（并行化基建）之后——两者叠加才是完整"研究组"。

---

## 8. D7 论文级交付 + PI 导师制

**现在的问题**：`report/generator.py` 输出静态模板 REPORT.md——结果表 + 树形图，没有摘要、相关工作、消融表、复现命令、限制声明。WebUI 只读（`webui/server.py` 明确 read-only）；人的参与只是"审批门"（interaction-modes），不是"共同研究"。

**改造内容**：

- **D7.1 树即论文（tree-as-paper）**：报告生成器升级为论文流水线：
  - **摘要**：从 instruction + 最终 insight 自动写；
  - **相关工作**：`node.related_work` / `node.grounding` 字段已存 alphaXiv 引用（`search_ctx.py` 基建已有），汇总成引用列表；
  - **方法**：树的可复现描述；
  - **消融表**：**树的每个分支天然是一次消融**——按 depth-1 家族聚合成分组消融表（这是别的 agent 框架给不出的、白赚的消融研究）；
  - **限制**：pruned 家族 + 红队攻击报告（D4.2）+ contamination 报告；
  - **复现**：每节点的 `code_ref` branch + eval 命令。
- **D7.2 引用纪律**：`TreeAddNodeTool`（`tree_ops.py:96`）在 IDEATE 时若 `grounding` 为空，强制一次轻量 `SearchIdeaContext`——"每个想法都有出处"从可选变成纪律（`idea-check` 已有此能力，接线进循环即可）。
- **D7.3 PI 导师制（双向）**：WebUI 从只读升级为"实验室控制台"，人类可以：
  1. 给节点贴**域先验**标签（"这个方向不可能，因为…"——作为约束写回 constraints 块）；
  2. 直接回答树上的**开放问题**（D2.4 的面板变成输入口，回答作为证据注入节点）；
  3. 用自然语言问树（`cli/companion.py` 从只读问答升级为"证据链问答"：为什么 3.1 失败？→ 返回失败档案 + 证据链）。
- **D7.4 可引用产物**：`arbor publish` 导出论文 markdown + 复现 tar（session 目录 + tree.json + code_refs），生成可引用快照。

**收益**：每次 run 产出可引用、可复现、带消融和限制的科学产物——这是"科研 agent"与"编码 agent"最本质的区分；人类从审批者变成共同研究者，域先验直接进树。

**风险**：论文生成可能"看起来很科学但空洞"——需要纪律：摘要必须引用树中真实节点，消融表必须来自真实分支；WebUI 双向化工作量不小。

**落点**：`report/generator.py`（重写）、`webui/server.py`（加输入通道）、`tree_ops.py`、`cli/commands/export_cmd.py`。**难度：中高**；D7.1/D7.2 的子集可以 P0 先做。

---

## 9. 优先级与落地路线

| 阶段 | 方向 | 理由 |
|------|------|------|
| **P0（低风险高价值，先做）** | **D3 统计严谨层**（多 seed + 效应量合并门） | 纯算法增强，不改协议，直接修正 AB 报告暴露的噪声问题 |
| | **D7.1/D7.2 树即论文**（消融表 + 引用纪律） | 报告生成是纯函数，零运行时风险，立刻提升交付物 |
| | **D4.3 失败博物馆**（剪枝转档案） | 改 `TreePruneTool` 一处，数据白赚 |
| **P1（结构性，中等风险）** | **D1 理论层 + 校准 + 惊奇** | 新字段 + 新状态，树结构小改；先离线用 replay 数据验证"预测校准"可行性 |
| | **D2 科研项目模式**（树 fork + 开放问题） | 数据结构小改 + CLI 参数，语义价值大 |
| | **D5.2 小模型蒸馏** | 消费已有 trajectory 数据，直接省钱 |
| **P2（范式级，高风险高回报）** | **D4.1/D4.2 红队验证** | 需先评估红队 LLM 的攻击能力 |
| | **D5.1/D5.3 元循环** | 把团队人工 A/B 产品化，依赖 P0 统计层 |
| | **D6 研究组模式** | 依赖 E27 并行化基建，最后做 |
| | **D7.3 PI 导师制** | WebUI 双向化，工作量最大，但差异化最强 |

---

## 10. 与既有文档的关系

- **与 E 系列（`OPTIMIZATION_INNOVATIVE.md`）**：D 系列不改"循环怎么转"（那是 E26/E27 的事），改"循环在理解什么、产出什么"。E 系列是 D 系列的基建：E1/E2 的 embedding 是 D4.3/D6.3 的度量基础；E5 的 EI 是 D1.1 的数值化；E14 的模型分层是 D5.2 的起点；E21 的 BudgetStage 是 D3.3 的家。
- **与 `OPTIMIZATION_FINDINGS.md`（增量清单）**：增量优化让"改造"跑得动——没有上下文管理、token 账目、缓存命中的改进，D1-D7 的每次实验都更贵。
- **与 roadmap**：D2 直接实现 Direction 1.1（长时程管理）与 1.3（自进化）；D7 实现 Direction 3.2（更好的导出与呈现）；D6 是 1.2（idea 质量评估）的组织化版本；D1 是 1.5（更强 agentic 研究能力）的答案。
- **与 AB_REPORT.md**：D3 的动机正是 AB 报告里"运行内报告与独立复测差异大"的统计缺口；D5.1 把这份报告的"人工 A/B 方法"变成框架内置能力。

---

## 11. 验证方法（延续 A/B 纪律）

每个方向落地后，按 `AB_REPORT.md` 的方法在 arbor-zoo 快任务上做对照：

- **有效性**：同模型同预算，基线 commit vs 改造 commit，比 dev/test 分数、产出密度（出分 executor 比例）、总时长；
- **统计**：至少 2-3 次独立复测（D3 落地后，框架自己就能给出置信区间）；
- **归因诚实**：区分"框架改造的收益"与"LLM 随机搜索的收益"（AB_REPORT §三的教训：同一模型两次运行的 idea 质量本身有方差）。

---

> **结论**：七个方向共享同一个北极星——**让 Arbor 从"把分数做高"进化为"把理解做深"**。P0 三项（D3 统计层、D7.1 消融论文、D4.3 失败博物馆）零协议改动、当天可做且立刻可 A/B 验证；P1 的 D1/D2 改变树的认知形态，是"科学共同体"的地基；P2 的 D5/D6 是最终形态，应排在既有 E 系列成果之后。
