# Arbor 优化组 vs 基线 A/B 报告

**任务**：k-NN AlgoTune 加速（`bash eval.sh dev` 迭代，`eval.sh test` 门控合并）
**对照组**：`214c5f5`（Windows worktree 修复）+ 无优化
**优化组**：`214c5f5` + `6fca246`（A4 token 估算缓存）+ `7e4bfa4`（executor 收敛）
**模型**：qwen3.8-max；配置相同（max_cycles=6, executor_max_turns=30, merge_threshold=5.0）

## 一、结果总表

| 指标 | 基线 | 优化组 | 变化 |
|---|---|---|---|
| dev 加速比（运行内报告） | 16.4x | 52.7x | +221% |
| test 加速比（运行内报告） | 18.23x | 43.97x | +141% |
| dev（本机独立复测） | — | **44.1x** | — |
| test（本机独立复测） | — | **53.5x** | — |
| 正确性 | PASS | PASS | — |
| 总时长 | 1h17m | 2h58m | +130% |
| 总输入 token | 7.72M | 11.64M | +51% |
| LLM 调用 | 253 | 276 | +9% |
| 缓存命中率 | 95.3% | 95.1% | ≈ |
| executor 跑到 30-turn 上限 | 4 / 6 | 3 / 7 | 改善 |
| executor 有产出（出分） | **2 / 6** | **7 / 7** | 质变 |

> 注：运行内报告的 52.7/43.97 与独立复测的 44.1/53.5 有差异——计时类指标在本机噪声较大，但量级（~44–53x）远超基线，结论不受影响。

## 二、executor 级明细

| 节点 | 基线 turns | 基线 tokens | 优化 turns | 优化 tokens | 优化分数 | 说明 |
|---|---|---|---|---|---|---|
| 1.1 | 21 | 359K | **10** | 151K | 8.98 | 收敛提前停，省 ~208K |
| 1.2 | 30 | 676K | **10+13** | 372K | 14.83 | 收敛提前停，省 ~304K |
| 3.1 | 30 | 612K | 30 | 1,036K | 23.35 | **收敛漏网**（见 §三） |
| 3.2 | — | — | 30 | 1,341K | 44.3 | 跑满但真产出，收敛正确地未打断 |
| 3.3 | — | — | 30 | 1,324K | 52.7 | 同上（最终 trunk） |
| 4.1 | 13 | 184K | 27 | 851K | 49.08 | 跑满但真产出 |
| 2.1/2.2 | 30/30 | 627K/939K | — | — | — | 基线空转到上限、无产出 |

## 三、因果诚实分析（关键）

**分数提升不是 A4/收敛直接造成的。** 两者都是效率优化（省 token/改循环行为），不改变 LLM 的搜索质量。52.7x 来自同一协调器（同一模型）这轮生成了更好的 idea（bucketized select、block-min tournament、homogeneous GEMM）。两次运行的 idea 生成是随机的，分数不可因果归因于框架修改。

**收敛优化的真实、可归因收益：**
- 1.1 / 1.2 提前收敛（21→10 turns；30→10+13 turns），合计 **省 ~18 turn / ~512K token**，彻底消除了基线里"提交工作代码后空转 A/B 侧实验"的经典浪费。
- 3.2 / 3.3 / 4.1 跑满 30 turn **不是收敛失效**——它们每几 turn 就提交真实改进，收敛正确地没有打断。这正是设计意图：只停空转，不停产出。
- **已确认的 gap（3.1）**：3.1 每 ~4 turn 提交一次 solution.py（同时跑 prof_*.py scratch 文件），idle 时钟反复被重置 → 收敛从未触发，烧掉 1.04M token 空转。**v2 候选启发式**：idle 检测不应因零星 solution.py 提交而重置——改为"仅在带 eval 分数提升的代码提交时重置"，或把 scratch 文件提交计为漂移证据。

**A4 的贡献：** 算法层面 12.1x（token 估算缓存，`_estimate_message_tokens`），但本任务端到端只省 ~3–5s——token 估算不是瓶颈。正确但在此任务上 E2E 影响可忽略（诚实结论，与用户此前质疑一致）。

**时长为何反增（2h58m vs 1h17m）：** 优化组把全部 6 个 cycle 用在了真探索上（7/7 executor 出分），而基线 6 个里 4 个空转到上限无产出。省下的 capacity 被协调器重新投入探索 → 总 token 更高、时长更长，但**产出密度质变**（7/7 vs 2/6 出分，最终结果 3 倍）。

## 四、结论

1. **收敛优化：有效，方向正确。** 消灭了 1.1/1.2 的空转（省 ~512K token），且能在高产出 executor 上正确让行。有一个已定位的 gap（零星提交重置 idle 时钟）。
2. **A4：算法正确，本任务 E2E 无感。** 属于"正确但低影响"的优化，GPU 服务器大上下文场景下价值可能更高（估算开销随消息数增长）。
3. **最终 trunk**：`a02f33a`（3.3 block-min tournament B=8），独立复测 dev 44.1x / test 53.5x，正确性 PASS。

## 五、v2 优化建议（优先级排序）

1. **收敛启发式 v2**：修复零星提交重置 idle 时钟的 gap（3.1 型）。预计可在不损失产出的前提下再省 1M token 级空转。
2. 基线里 4/6 executor 空转——若 v2 收敛普及到所有 executor，配合更早的"未达 merge_threshold 也允许 done"策略，可压缩时长。
3. A4 保留但不作为重点；GPU 部署时重新评估其大上下文收益。
