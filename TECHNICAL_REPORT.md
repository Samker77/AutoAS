# Arbor 自主研究智能体框架优化 —— 技术汇报

> 日期：2026-08-12 · 覆盖阶段：框架优化 + 实证验证（A/B）完成，GPU 服务器部署待进行
> 配套文档：`OPTIMIZATION_MAP.md`（架构地图）、`OPTIMIZATION_FINDINGS.md`（28 增量点）、`OPTIMIZATION_INNOVATIVE.md`（27 结构性创新）、`BASELINE_REPORT.md` / `AB_REPORT.md`（A/B 实证）

---

## 一、项目背景与目标

### 1.1 我们优化的是什么

**Arbor 是一个自主研究智能体框架**（中国人民大学 RUC-NLPIR 实验室主导、与微软研究院合作，Apache-2.0，arXiv 2606.11926，`src/` 约 4 万行 Python）。

它的运作方式：一个 **Coordinator（研究主管）** 反复执行 **观察→提假设→派活→收结果→总结→决策** 六步循环；每次派活让一个 **Executor（研究工程师）** 在**隔离的 git worktree** 里真正改代码、跑实验；只有通过**留出测试集**验证、且跨过可配置门槛的改进才合回主干。整个过程长成一棵**假设树**——结果和失败教训被持久化并向上传播，后续想法越提越聪明。

官方基准：在相同算力预算下，单一控制器在六项任务上全部超过 Claude Code / Codex **约 2.5×**（如 BrowseComp 67.67 vs 53.33；MLE-Bench Lite Any-Medal 86.36%）。

**我们做的不是"用这个框架跑一个任务"，而是优化这个框架本身**——让它用同样的算力预算做更多有效探索、少浪费。

### 1.2 为什么值得优化（动机）

自主研究智能体的运行成本由三部分组成：**LLM 调用次数 × 单次 token × 是否在热路径**。实测成本结构（本次 A/B 对照组，1 小时 17 分）：

- 单次运行消耗 **770 万输入 token**（95% 是前缀缓存命中）、229 次 LLM 调用；
- 6 个 Executor 中 **4 个空转到底**（撞 30-turn 上限），其中 **286 万 token 纯属浪费**——executor 提交了可用代码后，转头去跑 A/B 侧实验、微基准，直到把预算烧完。

框架自身存在可定位、可消除的浪费：token 估算每轮全量扫描、上下文压缩静默破坏缓存断点、报告解析/洞察传播每实验重复多次 LLM 调用、executor 无收敛机制空转、事件管道同步阻塞主循环……我们系统性识别出 **55 个优化点**（28 个增量 + 27 个结构性创新，全部精确到 `文件:行号`）。

**目标**：实现其中高价值优化 → 用严格的 A/B 在真实基准任务上实证其效果 → 部署到 GPU 服务器。

---

## 二、为什么选择 Arbor，而不是现成开源 Agent 框架

这是必须先讲清楚的决策。核心一句话：

> **我们的交付物是"框架能力的提升 + 可复用的实证证据"，不是"某个任务的答案"。因此选择标准不是"哪个框架开箱即用",而是"哪个框架允许我们进到它的循环内部、修改并量化影响"。**

### 2.1 对框架的需求清单（需求倒推）

| 编号 | 需求 | 为什么 |
|---|---|---|
| R1 | **源码级可修改核心循环** | 要优化的是 agent 循环内部行为（token 估算缓存、上下文压缩、循环终止/收敛逻辑），必须能改到核心代码，并能用 **commit 级开关做 A/B**（基线 commit vs 优化 commit） |
| R2 | **自带科研实验范式** | 假设树持久记忆、git worktree 隔离、dev/test 分离 + 合并门控、收敛检测——这是"让 AI 的探索可累积、有纪律、可追溯"的地基，缺失就得整体重造 |
| R3 | **完整可观测性** | A/B 方法论本身需要：token 账目、缓存命中率、事件日志、每 executor 的 turn/token/score。没有它们，"优化是否有效"无从量化 |
| R4 | **真实可部署** | 优化要落到实际用途——部署到 GPU 服务器跑真实任务，框架必须已经能稳定跑完整实验 |

### 2.2 主流开源 Agent 框架逐一对标

| 框架 | 定位 | 是否满足 R1（可改核心） | R2（研究范式） | R3（可观测） | 结论 |
|---|---|---|---|---|---|
| **AutoGen**（微软） | 多智能体会话编排 | △ 但核心是对话协议 | ✗ 无假设树/隔离/门控 | △ | 研究层要全重造 |
| **AutoGPT** | 单智能体自主任务 | △ | ✗ | △ | 同上 |
| **CrewAI** | 角色化多智能体 | △ | ✗ | △ | 同上 |
| **LangGraph / LangChain** | 编排原语（图/循环积木） | ✅ 低层可改 | ✗ 只是积木，无范式 | △ | 一切自己搭 = 变相自研 |
| **MetaGPT** | SOP 流水线（PM→工程→QA） | △ | ✗ 面向"一次成型"产品流程 | △ | 不做迭代式实证优化 |
| **OpenHands / SWE-agent** | 通用软件工程智能体 | △ | ✗ 无 dev/test 实验纪律、无假设树 | △ | 编码强、研究范式弱 |
| **Claude Code / Codex** | 通用编码工具 | ✗ 闭源 | ✗ 一次性滚动上下文、无持久记忆 | — | 正是 Arbor 要超越的基线 |
| **Sakana AI Scientist** | 模板化论文流水线 | △ | △ 有 idea→实验，但面向生成论文、模板化 | △ | 非通用优化目标框架 |
| **Arbor** | **自主研究智能体（假设树）** | ✅ Apache-2.0 源码全开 | ✅ 原生具备 | ✅ 事件/token/缓存全账目 | ✅ **选定** |

### 2.3 决策理由（四条）

1. **类别澄清：任务是"优化框架"，不是"用框架做任务"。** 如果目标是让 AI 完成一个任务，选现成框架是合理的；但我们的目标是把"研究智能体"这类系统本身做快——这必须能进到循环内部改代码并量化前后差异，而这恰恰是任何黑盒或未开源到内部的框架做不到的。

2. **需求倒推后只有 Arbor 全部命中（R1–R4）。** 其它框架要么缺研究范式（选它 = 把假设树、worktree 隔离、dev/test 门控、收敛检测全部重造一遍，成本与自研无异），要么缺可观测性（A/B 无法量化，优化验证无从谈起）。LangGraph 这类"积木"框架更接近自研起点的等价物，而非可复用的成品。

3. **为什么不自研：最难的部分 Arbor 已经实现并发表验证。** worktree 隔离 + 跨平台 git 正确性、假设树持久记忆、dev/test 实验纪律、收敛检测、事件/监控基础设施——这些是自主研究智能体里最难做对的部分。我们在它上面做的是**增量优化 + 实证**，而不是重新发明。本次 A/B 还暴露并修复了框架一个真实 bug（Windows 下 git worktree 创建失败，commit `214c5f5`）——这既说明框架需要工程投入，也说明我们的修改能力经得起实证检验。

4. **在最强开源的基线上再提速。** Arbor 是当前唯一把"假设树 + 实验纪律"做成开源框架并给出 2.5× 实证的项目。优化它，等于在最强的开源研究基线上再做增量——任何收益都直接落到我们后续要部署的真实用途上，不浪费。

> 一句话总结本节：**"不选现成框架"不是拒绝一切现成框架，而是只选那个满足 R1–R4 的框架（Arbor），并拒绝把时间和算力浪费在"用缺零件的框架重造研究范式"上。**

---

## 三、我们做了什么

### 3.1 两轮优化盘点

- **第一轮（增量）**：28 个优化点，分 4 类——LLM 调用层（成本影响最大）、执行流程层、持久化/事件/监控层、工具层与代码质量。每个都锚定到 `文件:行号`。
- **第二轮（结构性创新）**：27 个机会归为 7 大方向——语义化记忆、价值驱动调度、流水线化、成本分层、预算护栏、上下文架构、范式变革。核心主张：把 Arbor 从"单一 LLM 文本智能"升级为"语义记忆 + 价值调度 + 流水线并行"的研究系统，其中 P0 项均为"基建已就绪、只差接线"的零风险项。

### 3.2 本轮实际落地（3 个 commit）

| Commit | 内容 | 目标 |
|---|---|---|
| `214c5f5` | **Windows worktree 修复**：`git` 经 argv 数组调用，不走 cmd.exe 拼字符串 | 让框架在本机（Windows）能创建隔离 worktree——A/B 两侧共用的前置条件 |
| `6fca246` | **A4 增量 token 估算缓存**：消息级缓存估算值，追加增量更新、压缩时失效 | 消灭每回合对全部消息的 O(N) 全量 `tiktoken.encode` 扫描（微基准 **12.1×**） |
| `7e4bfa4` | **Executor 收敛**：提交工作代码后若持续空转（跑 A/B 侧实验），循环层 nudge + 提前收尾（`stop_reason="converged"`） | 消灭基线里"提交后空转到 30-turn 上限"的经典浪费 |

### 3.3 A/B 验证设计（为什么这样能证明）

同一台机器、同一个任务（kNN AlgoTune 加速）、同一个模型（qwen3.8-max）、**完全相同的指令与配置**，只改变一个变量：

- **对照组** = `214c5f5`（未修改框架）；
- **优化组** = `214c5f5` + A4 + 收敛。

两次都在任务仓库 `baseline_algotune` 上跑完整实验（该任务：只允许改 `solution.py`，把 2000×16 维 k-NN 暴力搜索加速，`eval.sh dev` 打印加速比、`is_solution` 门控正确性、`eval.sh test` 门控合并）。

### 3.4 A/B 结果（含诚实归因）

| 指标 | 对照组 | 优化组 | 说明 |
|---|---|---|---|
| 最终加速比 dev / test | 16.4x / 18.23x | 52.7x / 43.97x | 独立复测 dev≈44x / test≈53x，正确性均 PASS |
| executor 出分占比 | **2/6** | **7/7** | 对照 4 个 executor 空转到上限无产出 |
| 撞 30-turn 上限的 executor | 4/6 | 3/7 | — |
| 总时长 | 1h17m | 2h58m | 优化组把预算全部投入了真探索 |
| 总输入 token | 7.72M | 11.64M | 95% 前缀缓存命中 |

**诚实归因（关键）：**

- **分数 3 倍提升不能归因于框架优化。** 分数由协调器的 idea 搜索质量决定，两次运行的 idea 生成有随机性。框架优化改变的是**效率**（省 token、停空转），不是搜索能力。这点必须讲清楚，不能把随机收益记到优化头上。
- **收敛优化可归因的收益：** 1.1 / 1.2 节点提前收尾（21→10 turns；30→10+13 turns），**合计省约 18 turn / 51 万 token**，彻底消除"提交后空转 A/B 侧实验"的浪费；且在高产出的 3.2/3.3/4.1 节点上**正确地没有打断**（它们每几 turn 提交真实改进，收敛本就不该停）。
- **已知 gap：** 节点 3.1 因每 ~4 turn 提交一次 `solution.py`，idle 时钟反复重置，收敛未触发，空转 104 万 token。→ 已列为 v2 改进（"仅带分数提升的提交才重置 idle 时钟"）。
- **A4 可归因的收益：** token 估算路径微基准 12.1×；但本任务上 token 估算不是瓶颈，端到端仅省数秒。属于"算法正确、本任务 E2E 无感"，GPU 部署大上下文场景需重估。

### 3.5 结论与下一步

优化方向的**核心收益**是：把 executor 从"空转烧预算"拉回"提交即收尾"，让同一预算做更多有效探索（对照组 4/6 executor 空转、优化组 7/7 有产出）。这直接服务于 Arbor 自身的卖点——**同样算力预算下出更多结果**。

下一步：

1. **收敛启发式 v2**：修复 3.1 型 gap（零星代码提交重置 idle 时钟），预计再省约百万 token 级空转，需先在回归测试套件里补 3.1 场景再合入。
2. **部署 GPU 服务器**：把优化后的框架部署到 GPU 服务器，跑真实训练/推理类优化任务，届时重估 A4 在大上下文下的收益。

---

## 四、一句话总结

> 我们把"自主研究智能体框架"这个系统本身作为优化对象，选择了唯一满足"源码可改 + 自带研究范式 + 完整可观测 + 可部署"四项需求的 **Arbor**（而非在缺零件的现成框架上重造研究层），用**同一任务、同一模型、仅切换优化 commit** 的 A/B 实证了优化效果：收敛机制把 executor 从空转拉回正轨、A4 消除了 token 估算热点，并诚实区分了"框架可归因的收益"与"搜索随机性带来的分数"。
