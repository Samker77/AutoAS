# 搜索以及小模型探索

> 背景：为 ResearchSearch（grounded ideation）设计测试任务（方法②：大尺度 ANN，暂不实现），
> 并回答三个问题——如何保证搜出来的东西有用、没用怎么办、搜索模型能否换轻量小模型降本；
> 最后诚实梳理整个项目里"科研"环节哪些模块适合用 小模型 降本。
>
> 全部结论基于对 `src/` 的实际读码与一次真实 run 的 token 统计，不是纸面推演。

---

## 0. 测试任务：方法②（为什么它适合测 ResearchSearch）

把现有 k-NN 精确搜索任务放大到 **ANN 尺度**（`n_db` 从 2,000 提到 10⁶–10⁷、`dim` 提到 128），
精确方法不可行 → agent 必须在 IVF / PQ / HNSW / 图方法 / 量化之间**做选择**，
而这条线有真实可查的文献（HNSW'18、NSG'19、DiskANN'19 + 2024–25 量化/图方法改进）。
**知识鸿沟真实存在**（"这个尺度下哪个方案好 + 2025 有什么改进"需要查），且结果可量化（recall@k + 延迟）。

判定标准：先把任务给一个**禁用搜索**的 run，如果它干净地赢了，说明任务不适合测搜索价值。

---

## 1. 如何保证搜出来的东西有用

### 1.1 设计里已有的保证（读码核实）

| 机制 | 位置 | 作用 |
|---|---|---|
| **intent 塑造查询** | `research_ctx.py` | ResearchSearch 有 4 种 intent（`related_work` / `survey` / `lookup` / `explore`），协调器按知识鸿沟类型选，搜的方向被约束 |
| **隔离上下文 + 诚实引用** | `_agent_recover.filter_sources_to_visited` | digest 只保留**真正打开过**的源，没打开的一律丢弃——引用必然有真实内容背书，杜绝"编来源" |
| **按通路控制读深** | `visit_max_content_tokens=2048` / `research_visit_tokens=6000` | grounded ideation 读到 method/results 全文，novelty audit 只读摘要，深度匹配用途 |
| **grounding 落点是可选** | `TreeAddNode` 写 `node.grounding` | 只有当某个源**真的塑造了想法**才记录——没用的搜索不留痕，天然过滤 |
| **失败显式标记** | `[search-failed: ...]` 标记 | 后端不可用/无结果/异常都有明确标记，协调器能识别并忽略 |

### 1.2 缺失的、值得补的机制

1. **近期性偏置（最优先补）**。alphaXiv 结果**不按时间过滤**——搜 "kNN search" 会同时返回 2010 和 2025 的论文。SearchAgent 的 prompt 应默认强调"优先近 3 年 + 高引用"，或用 `focus` 显式带 `prefer arxiv 2024+`。这是 prompt 级改动，零成本。
2. **用途可观测性**。目前没有自动化信号衡量"检索到底有没有进决策"。建议在 run 结束后统计：`grounding` 非空的节点数、最终报告里被引用的 grounding 数、以及 **grounding 与 outcome 的相关性**（有 grounding 的 idea 是否更常 merged / 更常超 trunk）。没有相关性 → 检索是噪音。
3. **查询分解质量**。SearchAgent 用 hypothesis 直接出查询；hypothesis 越模糊检索越差。应引导 SearchAgent 把 hypothesis 拆成 2–3 个具体子查询（`agent_max_turns=12` 留了空间），而不是单查询撞运气。
4. **A/B 度量**（方法② 的核心用途）。同一 ANN 任务跑 `grounded_ideation: false` vs `true` 各一轮，对比最终分数 + 赢的方法是否被 grounding 引用。这是"有用"的最终判定。

### 1.3 保证链条（小结）

```
intent 约束 → 隔离检索 → 诚实引用 → 落点可选 → 可观测统计 → A/B 判定
   塑造       防污染      防编造       天然过滤      量化信号     最终裁决
```

缺一环就缺一截证据：没有 A/B，前面的机制再好也只能说"检索没坏"，不能说"检索有用"。

---

## 2. 如果没用怎么办

### 2.1 先判断"没用"（全部可量化）

| 信号 | 读数 | 含义 |
|---|---|---|
| **grounding 引用率** | 最终报告/merged idea 里 grounding 被引用 = 0 | 检索没进决策 |
| **grounding→outcome 相关** | 有 grounding 的 idea 并不比无 grounding 的更常超 trunk | 检索是噪音 |
| **digest 质量** | 频繁空结果 / digest 极短 / 全是通用描述 | 检索没采到东西 |
| **调用频率** | 协调器几乎不调 ResearchSearch | 工具没被用（模型不伸手 / 任务不需要） |

### 2.2 应对阶梯（从便宜到釜底抽薪）

1. **调 prompt（零成本）**：加近期性偏置、改进 intent/focus 措辞、引导查询分解。
2. **换/加后端**：alphaXiv 只有论文。如果鸿沟在工程实现技巧（GitHub / 博客）而非论文，加 `jina`（当前机器连不上 `s.jina.ai`，网络通后一行启用）或其他 web 后端。
3. **降级通路**：关掉贵的按需检索（`grounded_ideation: false`），只留便宜的实验前新颖性审计（`auto_search_on_add: true`，后台、只读摘要、advisory）。
4. **切掉（可逆）**：`search:` 整个块移除或 `enabled: false`。搜索是**附加件不是承重墙**——协调器没有它照常跑，这是它最大的优点。
5. **诚实裁决**：如果 A/B 显示零差异，结论就是"这个任务族不需要文献"。保留搜索给真正的知识鸿沟任务（方法①），不为它有而用它。

---

## 3. 搜索模型能否换轻量小模型？——能，且已是一等配置项

**答案是：能，`search.agent_model` 已经实现**（`search_agent/agent.py` 的
`_maybe_override_provider` 会为 SearchAgent 建一个**独立 provider**，用你指定的模型）。

```yaml
search:
  agent_model: "<轻量模型名>"   # None = 复用协调器模型
  agent_max_turns: 12          # 检索循环轮次上限（成本/质量的旋钮）
  agent_timeout: 120           # 单次检索墙钟上限
```

### 为什么这个换模型比看起来更值（token 分布证据）

真实 run（`run_20260812_092700`，qwen3.8-max）的统计：

| 指标 | 数值 |
|---|---|
| 全部输入 token | 7,718,728（**95.3% 是缓存命中**） |
| 未缓存输入 | 366,408 |
| 全部输出 token | 181,902 |
| LLM 调用 | 253 |
| 协调器自身缓存占比 | 96.7% |

推论：**项目成本大头是"缓存命中的输入"（很便宜）+ 输出 token**。而 SearchAgent 每次都是
**全新 provider + 隔离上下文**，它的 token 恰恰是**未缓存的贵那种**，无法摊到协调器的缓存里。
所以给 SearchAgent 换小模型是**双重省钱**：(a) 每 token 单价低；(b) 打的正是未缓存的贵 token。

### 权衡与推荐

- **SearchAgent 要做工具调用 + 综合摘要**，模型太弱会掉工具调用、产出浅 digest。推荐用一个"工具调用可靠、足够快"的档位（如 claude-haiku 级或便宜的 qwen 变体），而不是最小的玩具模型。
- **ResearchSearch 是阻塞式**（digest 同步进协调器上下文）——小模型若同时更快，还能减协调器等待时间。
- **风险很低**：检索产物是 advisory 的 digest + grounding，不进实验决策主干；最坏情况就是"搜得浅"，有 `[search-failed]` 标记兜底。

---

## 4. 全项目小模型降本探索（诚实版）

### 4.1 模型接线现状（读码核实）

- `effective_meta_model = meta_model or llm.model`（`config.py:577`）。
- **协调器 + executor 共享** `effective_meta_model`（`main.py:102`、`orchestrator.py:623`、`executor_run.py` 直接复用传入的 provider）——**目前没有独立的 executor 模型开关**。
- **唯一现成的独立模型开关是 `search.agent_model`**。
- intake / distill / propagate 全部复用协调器 provider，低频一次性。

### 4.2 各模块评级

| 模块 | 调用量 | 缓存摊薄 | 换小模型价值 | 风险 | 结论 |
|---|---|---|---|---|---|
| **SearchAgent（检索）** | 每次全新上下文 | **否**（未缓存） | **最高** | 低（advisory） | **✅ 立即做**，旋钮现成 |
| **Executor（跑实验）** | 高（~3.3M in/run） | 部分 | 中 | **中高**：做真实文件修改，出错烧墙钟、污染树；有 eval.sh 正确性门兜底但无法完全兜住 | ⏳ **潜在，需设计**：先加独立的 executor 模型字段 + 用较强廉价模型做 A/B，再考虑 |
| **Distill（蒸馏）** | 低频、可选（`distill_abstract`） | 否 | 低 | 低 | 顺手可配，收益小 |
| **Propagate（洞察传播）** | 每次 merge 一次摘要 | 否 | 低 | 低 | 顺手可配，收益小 |
| **Intake（规划）** | 一次性 | 否 | 低 | 低 | 收益小，不值得动 |
| **Coordinator（编排）** | 最大（4.4M in） | **是（96.7%）** | **低** | **高**：编排质量是全系统的天花板 | **❌ 不值得**——缓存已把输入压到几乎免费，省的是最便宜的部分，风险却是全局的 |

### 4.3 诚实结论

> 整个项目里，"科研"环节通过小模型降本的**唯一干净落点就是 SearchAgent**——既有现成旋钮
> （`agent_model`），又打在最贵的未缓存 token 上，风险还是最低的。
>
> **Executor 是最有想象空间、但今天做不了**的第二个候选：需要先加独立模型开关，且要用较强
> 的廉价模型做严格 A/B（正确性门兜底 + 最终分数对比）才敢启用。这是一个"值得立项、不是顺手配置"的事。
>
> **Coordinator 不值得**：96.7% 输入被缓存压到几乎免费，省小模型差价省的是最便宜的部分，而编排质量下降是全局性风险。**没有硬编**——这三档就是读完代码后的真实排序。

---

## 5. 附录：方法② 任务参数草稿（暂不实现）

保持现有 harness（`task.py` / `eval.py` / `eval.sh` / `is_solution` 契约）不变，只改：

- `n_db`: 2,000 → **1,000,000**（近似搜索进入必要区）
- `n_query`: 200 → 1,000
- `dim`: 16 → **128**
- `k`: 10 不变
- 评测：**recall@k 门（≥ 阈值才判正确）+ 延迟中位数**，防止"快但错"
- 数据仍随机生成（不落盘），种子固定以保 A/B 可复现
- 单实例规模增大后，5 次计时中位数的稳定窗口要重新标定（大矩阵的计时噪声特性不同）

配套观测：`node.grounding` 非空率、grounding→merged 相关性、最终报告中引用 grounding 的次数。

---

### 5.1 实际落地校准（2026-08-19，基准实测）

草稿按 10⁶ 设计，实际在评测机（Windows 11 / 8 逻辑核 / 单线程 numpy）上标定后做了三处**实测驱动的修正**，全部有数据依据：

| 草稿 | 实际 | 原因（实测） |
|---|---|---|
| `n_db = 10⁶` | **250,000** | 精确参考（numpy 分块 GEMM）在 10⁶ 下单线程 ~30-90s，单次 eval 不可接受（68 次 eval/run → 数小时）；250k 下参考 ~1.2-1.3s，单次 eval ~15-20s，全程可跑。 |
| 任意 ANN 库 | **faiss-cpu** | hnswlib 在 Python 3.14/Windows 无 wheel 且本机无 MSVC 编译链；faiss-cpu 1.15 有 cp314 wheel。且本机 **faiss HNSW 构建异常慢**（100k 单线程 158s / 默认线程 36s），而 **IVF-Flat 快**（250k 构建 ~5s）→ 最优解实际是 IVF-Flat，HNSW 在此硬件上不可用。这本身就是一个"搜索才有用"的实证点。 |
| `eval.sh` 单线程锁核 | **OMP 解锁 / OPENBLAS 仍锁 1 线程** | 单线程下索引构建是灾难（见上）；解锁 OMP 后 IVF 构建 5s。numpy 参考仍锁 1 线程，分母稳定（~1% 抖动）。 |
| 数据：随机正态 | **嵌入式的簇状数据**（10pt/簇、center×6、噪声 σ=1.5） | 纯 i.i.d. 高斯 128 维下**所有 ANN 方法召回都差**（HNSW 最多 0.78，距离集中）。真实 ANN 数据（SIFT/glove）有簇结构。实测：10pt/簇 数据下 IVF 召回随 nprobe 真实变化（nprobe=2→0.87、5→0.90、10→0.92、60→0.97），是合格的 tuning 旋钮。 |

最终参数：`n_db=250_000, n_query=200, dim=128, k=10, RECALL_GATE=0.90`。

**验证结果**（基线 / 可行解 / 门禁）：

| 方案 | recall@k | score |
|---|---|---|
| 精确 brute force（初始 solution.py） | 1.000 | **~1.0×**（≈参考平手） |
| faiss `IndexIVFFlat(nlist=600, nprobe=10)` + 模块级索引缓存 | 0.91（过门） | **~32×** |
| 同上 nprobe=2（召回不足） | 0.87（<0.90） | **0.0**（门禁拦截） |
| 随机索引 | — | **0.0**（门禁拦截） |

**已知与草稿的偏差，诚实说明**：250k 下"精确可行（~1s）、只是慢"——强制力比 10⁶ 弱，但知识鸿沟仍在：最优解必须发现 faiss + 选对索引族 + 调 nprobe/nlist 过 0.90 召回门 + 最小化延迟；而"快但错"和"召回不足"都会被门禁判 0。A/B 的裁决信号（最终分 32× vs 1×）远大于 ~1% 计时噪声，成立。
